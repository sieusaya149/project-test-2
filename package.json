{
  "name": "zalo-clone",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "coverage": "node --test --experimental-test-coverage --test-coverage-lines=80"
  }
}
